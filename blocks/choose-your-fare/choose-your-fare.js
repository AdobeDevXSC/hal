// EDS block entry — auto-loaded for a "choose-your-fare" block.
// Mounts shared-ui <ChooseYourFare> as a React island (see ../_react/block.js).
import { renderBlock } from '../_react/block.js';

export default (block) => renderBlock(block, 'ChooseYourFare');

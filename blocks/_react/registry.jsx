// name -> shared-ui React component. Add entries here as blocks are built.
// Keep this list minimal: every component listed is bundled into react-runtime.js.
import { Button } from '@hal-sbn-root/shared-ui';

export const registry = {
  Button,
};

// EDS block entry — auto-loaded for a "headline" block.
// Mounts shared-ui <Headline> as a React island (see ../_react/block.js).
import { renderBlock } from '../_react/block.js';

export default (block) => renderBlock(block, 'Headline');
